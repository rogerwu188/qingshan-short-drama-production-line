# Examples

Start with `configs/pipeline.example.json` and create a private runtime
workspace with `qingshan init`. Episode manifests contain story, identity and
provider references, so real production examples are intentionally not
published. The test fixtures under `tools/tests/fixtures` are synthetic and
safe to use when learning individual gate schemas.
