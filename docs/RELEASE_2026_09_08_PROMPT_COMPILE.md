# v2026.09.08.2 — Separate planned prompts from actual-media admission

Includes v2026.09.08.1 mandatory batch checks plus explicit preproduction-only compilation for both model families. Planned prompt compilation can precede first-frame generation without claiming visual PASS. Actual submission compilation stays strict and structural prop checks remain mandatory. The default provider text is unchanged.

Validated: 20 focused tests, including unchanged default text, source immutability, no task-flag bypass, strict final rejection after draft compilation, and ownership/structural failures in draft mode. No paid generation or publication was used for these tests.
